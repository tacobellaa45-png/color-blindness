Color Blindness Test is a Python program that displays Ishihara-style images and asks the user to identify the hidden number. 
The program checks the answers and calculates the user's accuracy score.

Technologies Used:
- Python
- OpenCV
- NumPy
